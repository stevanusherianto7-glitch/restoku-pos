/**
 * PengaturanToko/Index.tsx — DEPRECATED
 *
 * Halaman ini telah digantikan oleh PengaturanOutlet/Index.tsx yang terhubung
 * ke database sungguhan (Tenant + Outlet + Users) via Inertia props dan
 * router.put(). Halaman ini TIDAK LAGI DIGUNAKAN.
 *
 * Route /pengaturan-toko di backend sudah di-redirect 301 ke /pengaturan-outlet.
 * File ini dipertahankan sementara sebagai referensi, tapi bisa dihapus.
 */

export { default } from '../PengaturanOutlet/Index';
